name: Run a scheduled job

on:
  schedule:
    - cron:  '*/15 * * * *'

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: echo a string
        run: echo "Hello world"
    